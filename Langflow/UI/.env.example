# Public base URL of your Langflow server (no trailing slash).
# Leave blank for local dev — the Vite proxy forwards /api to http://localhost:7860.
# In production (Vercel), set this to a publicly reachable Langflow URL,
# e.g. a cloudflared/ngrok tunnel or a hosted instance.
VITE_LANGFLOW_URL=
