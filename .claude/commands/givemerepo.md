Run the following bash command and show me the git commit history for this repository:

```bash
git log --oneline --all
```

Present the results clearly, listing each commit with its hash and message. Also show the current branch with `git branch` and the remote info with `git remote -v` if any remotes exist.
